// TEMPORARY: the real bookmarklet source is stubbed out to test whether GitHub Pages
// was rejecting the deploy because of the bundled bookmarklet content (a javascript:
// URL reading the session cookie and calling Instagram's private API).
// Restore the real version with:  git checkout 9ef0a9f -- src/features/instagram/bookmarklet.ts
export const BOOKMARKLET_HREF = ''
